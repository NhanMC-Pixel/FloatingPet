package com.floatingpet.domain

/**
 * All possible states of the pet.
 * Default assets: a colored circle + body text representing the state.
 */
enum class PetState {
    IDLE,
    WALK,
    SLEEP,
    DRAG,
    DANCE,
    HAPPY,
    SAD
}